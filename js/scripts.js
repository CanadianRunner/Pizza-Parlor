//Business Logic

function Pizza {
  constructor(size, toppings, crust) {
    this.size = size,
    this.toppings = toppings,
    this.crust = crust
  }

Pizza.prototype.costCalculation = (pizzaArray, )
}